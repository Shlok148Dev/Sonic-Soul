version: "3.9"

services:
  psyche-api:
    build:
      context: ..
      dockerfile: docker/Dockerfile
    ports:
      - "8000:8000"
    environment:
      - SUPABASE_URL=${SUPABASE_URL}
      - SUPABASE_ANON_KEY=${SUPABASE_ANON_KEY}
      - UPSTASH_REDIS_URL=${UPSTASH_REDIS_URL}
      - UPSTASH_REDIS_TOKEN=${UPSTASH_REDIS_TOKEN}
      - PSYCHE_API_KEY=${PSYCHE_API_KEY}
      - WANDB_API_KEY=${WANDB_API_KEY}
      - OLLAMA_HOST=http://ollama:11434
    volumes:
      - ../data:/app/data
      - ../configs:/app/configs
    depends_on:
      - ollama
    restart: unless-stopped

  ollama:
    image: ollama/ollama:latest
    ports:
      - "11434:11434"
    volumes:
      - ollama_data:/root/.ollama
    restart: unless-stopped

  # psyche-ui:
  #   build:
  #     context: ../psyche-ui
  #     dockerfile: Dockerfile
  #   ports:
  #     - "3000:3000"
  #   environment:
  #     - NEXT_PUBLIC_PSYCHE_API_URL=http://psyche-api:8000
  #     - NEXT_PUBLIC_PSYCHE_API_WS=ws://psyche-api:8000
  #   depends_on:
  #     - psyche-api

volumes:
  ollama_data:
